<turbo-frame id="savings_table_resume">
  <div class="bg-gray-700 p-6 rounded-lg shadow-lg">
    <h3 class="text-xl font-bold text-green-400 mb-3 flex items-center gap-2">
      <i class="fas fa-dollar-sign"></i> Resumo do Progresso
    </h3>
    <p class="text-gray-300">
      Vocês já completaram 
      <strong><%= savings_table.savings_table_squares.where(checked: true).count %></strong> quadrados, 
      restam <strong><%= savings_table.savings_table_squares.where(checked: false).count %></strong> para atingir a meta!
    </p>
  </div>
</turbo-frame>